services:
  # ── Backend (Node.js API) ──────────────────────────────────────────────────
  - type: web
    name: salestrack-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node src/index.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        sync: false          # set manually in Render dashboard
      - key: JWT_EXPIRES_IN
        value: 30m
      - key: FIREBASE_PROJECT_ID
        value: room-4cf55
      - key: FIREBASE_CLIENT_EMAIL
        sync: false          # set manually
      - key: FIREBASE_PRIVATE_KEY
        sync: false          # set manually
      - key: VAPID_PUBLIC_KEY
        value: BD0ED9G1sSuUdy-QKBHwU3vu5YA-c62QJHiN2tXpamD258fX8GD93o2A6I6SbDgvd-XU6DjHJkWfT7FSRjVMEX0
      - key: VAPID_PRIVATE_KEY
        sync: false          # set manually
      - key: VAPID_SUBJECT
        value: mailto:admin@salestrack.com
      - key: FRONTEND_URL
        sync: false          # set after frontend is deployed

  # ── Frontend (Static Site) ─────────────────────────────────────────────────
  - type: web
    name: salestrack-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    plan: free
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_FIREBASE_API_KEY
        sync: false          # set manually
      - key: VITE_FIREBASE_AUTH_DOMAIN
        value: room-4cf55.firebaseapp.com
      - key: VITE_FIREBASE_PROJECT_ID
        value: room-4cf55
      - key: VITE_FIREBASE_STORAGE_BUCKET
        value: room-4cf55.appspot.com
      - key: VITE_FIREBASE_MESSAGING_SENDER_ID
        value: "118412863960313180091"
      - key: VITE_FIREBASE_APP_ID
        sync: false          # set manually
      - key: VITE_API_URL
        sync: false          # set after backend is deployed
